[build-system]
requires = ["hatchling>=1.27.0"]
build-backend = "hatchling.build"

[project]
name = "moeptimizer"
version = "0.7.27"
description = "Transparent OpenAI API proxy that optimizes context for MoE + MTP models in multi-turns agentic tasks."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [
    { name = "Abel", email = "abel@example.com" },
]
keywords = ["moe", "agentic", "llm", "middleware", "fastapi", "rag", "lancedb", "mtp", "speculative-decoding"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "fastapi>=0.141.1",
    "uvicorn[standard]>=0.52.0",
    "openai>=2.52.0",
    "httpx2>=2.9.1",
    "numpy>=2.4.6",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
    "tiktoken>=0.13.0",
    "pygments>=2.20.0",
    "tree-sitter>=0.26.0",
    "tree-sitter-language-pack>=1.13.7",
    "lancedb>=0.36.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.1",
    "mypy>=2.3.0",
    "httpx2[http2]>=2.9.1",
    "requests>=2.34.2",
]
prediction = [
    "xgboost>=3.2.0",
]
all = [
    "xgboost>=3.2.0",
]

[project.scripts]
moeptimizer = "moeptimizer.__main__:main"
moeptimizer-config-check = "moeptimizer.config_check:main"

[tool.hatch.build.targets.wheel]
packages = ["src/moeptimizer"]

[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "SIM", "RUF"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = ""

[tool.mypy]
python_version = "3.11"
strict = true
warn_return_any = true
warn_unused_configs = true